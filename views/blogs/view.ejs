<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta
      name="viewport"
      content="width=device-width, initial-scale=1, shrink-to-fit=no"
    />
    <title>Blog Details</title>

    <!-- Bootstrap CSS CDN -->
    <link
      rel="stylesheet"
      href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css"
    />

    <!-- Tailwind CSS CDN -->
    <link
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
      rel="stylesheet"
    />
  </head>
  <body class="bg-gray-100">
    <div class="container mx-auto my-8">
      <% if (blog) { %>
      <div class="bg-white rounded-lg shadow-md overflow-hidden">
        <div class="p-6">
          <h2 class="text-3xl font-semibold mb-4 text-gray-800">
            <%= blog.title %>
          </h2>
          <a
            href="/blogs"
            class="btn btn-primary bg-green-500 hover:bg-blue-700 text-white"
          >
            All atricle
          </a>
          <p class="text-sm text-gray-600">
            Author: <%= blog.author.username %>
          </p>
          <p class="text-sm text-gray-600">
            Description: <%= blog.description %>
          </p>
          <p class="text-sm text-gray-600">
            Published on: <%= blog.createdAt.toDateString() %>
          </p>
          <hr class="my-4" />
          <p class="text-gray-800"><%= blog.content %></p>

          <!-- Edit and Delete Buttons -->
          <div class="flex justify-between mt-4">
            <a
              href="/blogs/<%= blog.slug %>/edit"
              class="btn btn-primary bg-blue-500 hover:bg-blue-700 text-white"
            >
              Edit
            </a>
            <!-- Delete Button Form -->
            <form
              action="/blogs/<%= blog._id %>?_method=DELETE"
              method="post"
              onsubmit="return confirm('Are you sure you want to delete this blog?');"
            >
              <button
                type="submit"
                class="btn btn-danger bg-red-500 hover:bg-red-700 text-white"
              >
                Delete
              </button>
            </form>
          </div>
          <!-- End Edit and Delete Buttons -->
        </div>
      </div>
      <% } else { %>
      <div class="alert alert-danger mt-4" role="alert">Blog not found.</div>
      <% } %>
    </div>

    <!-- Bootstrap JS and jQuery CDN -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.0.9/dist/umd/popper.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
  </body>
</html>
