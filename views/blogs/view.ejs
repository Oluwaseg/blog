<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Blog Detail</title>

    <!-- Bootstrap CSS CDN Link -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    />

    <!-- Tailwind CSS CDN Link -->
    <link
      rel="stylesheet"
      href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css"
    />
  </head>

  <body class="bg-gray-100">
    <div class="container mx-auto mt-8">
      <div class="bg-white p-8 shadow-md rounded-md">
        <% if (blog) { %>
        <h2 class="text-3xl font-semibold mb-4"><%= blog.title %></h2>
        <p class="text-gray-600">Author: <%= blog.author.username %></p>
        <p class="text-gray-600">Description: <%= blog.description %></p>
        <p class="text-gray-600">
          Published on: <%= blog.createdAt.toDateString() %>
        </p>
        <hr class="my-4" />

        <div class="mb-8">
          <p class="text-gray-800"><%= blog.content %></p>
        </div>

        <!-- Edit and Delete Buttons -->
        <div class="flex items-center space-x-4">
          <a href="/blogs/<%= blog._id %>/edit" class="btn btn-primary">
            Edit
          </a>
          <!-- Delete Button Form -->
          <form
            action="/blogs/<%= blog._id %>?_method=DELETE"
            method="post"
            onsubmit="return confirm('Are you sure you want to delete this blog?');"
          >
            <button type="submit" class="btn btn-danger">Delete</button>
          </form>
        </div>
        <!-- End Edit and Delete Buttons -->

        <% } else { %>
        <div class="alert alert-danger" role="alert">Blog not found.</div>
        <% } %>
      </div>
    </div>

    <!-- Bootstrap JS and Popper.js CDN Links (Include them at the end of the body) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
  </body>
</html>
